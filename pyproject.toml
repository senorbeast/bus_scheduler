[project]
name = "bus-scheduler"
version = "0.1.0"
description = "Streamlit discrete-event bus charging scheduler."
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "pyyaml>=6.0",
    "streamlit>=1.35.0",
]

[tool.uv]
package = false
